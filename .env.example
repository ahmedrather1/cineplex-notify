# --- Server ---
PORT=3001
# Public URL of the deployed app (used for unsubscribe links in emails)
PUBLIC_BASE_URL=http://localhost:3001

# --- Database (Railway/Render inject DATABASE_URL when you add the Postgres addon) ---
DATABASE_URL=postgres://postgres:postgres@localhost:5432/cineplex_notify
# Set to "disable" for local Postgres without SSL
PGSSL=disable

# --- Poller ---
ENABLE_POLLER=0
# Fast lane (coming-soon films — cheap bulk fetch, latency-critical advance on-sales)
FAST_POLL_SECONDS=30
# Slow lane (now-playing films — large payloads, less time-critical)
SLOW_POLL_MINUTES=5

# --- Email (any SMTP provider: Resend, Mailgun, SES, Mailpit locally) ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Cineplex Alerts <alerts@example.com>"
# Optional: a donation/support link. When set, emails include a small
# "support hosting costs" line pointing here (Ko-fi, Buy Me a Coffee, etc.).
DONATE_URL=

# --- Cineplex (public key from the website; override only if it rotates) ---
# CINEPLEX_API_KEY=
